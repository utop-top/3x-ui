name: Sync Upstream

on:
  schedule:
    # 每 6 小时自动检查并同步一次上游变动
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  sync-upstream:
    runs-on: ubuntu-latest
    steps:
      - name: Sync with upstream using official GitHub CLI
        env:
          GITHUB_TOKEN: ${{ secrets.SYNC_PAT }}
        run: |
          gh repo sync ${{ github.repository }} --source MHSanaei/3x-ui --branch main
